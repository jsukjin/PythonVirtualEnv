# how to set up
# 첨부파일 :  n8n docker-compose.yml / searXNG setting file

# how to setup
# 1.docker-compose.yml에 searXNG 추가
# 2.env에 로컬주소 등록
# 3. searxng 폴더 만들고 그안에 settings.yml 넣기
# 4. docker에 올리기 (같은 네트워크 기준)